How to setup project: https://www.youtube.com/watch?v=mr9Mtm_TRpw

How to start project:
1.) Navigate to project folder
2.) type "npm start" to start up app

<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Word Creator</title>
  </head>
  <body>
    <h1> App Name </h1>
  </body>
  <script>
          function startDrag(e) {
              // determine event object
              if (!e) {
                  var e = window.event;
              }

              // IE uses srcElement, others use target
              var targ = e.target ? e.target : e.srcElement;

              if (targ.className != 'dragme') {return};
              // calculate event X, Y coordinates
                  offsetX = e.clientX;
                  offsetY = e.clientY;

              // assign default values for top and left properties
              if(!targ.style.left) { targ.style.left='0px'};
              if (!targ.style.top) { targ.style.top='0px'};

              // calculate integer values for top and left
              // properties
              coordX = parseInt(targ.style.left);
              coordY = parseInt(targ.style.top);
              drag = true;

              // move div element
              //object = document.querySelectorAll('div[title="sh"]');
                  document.onmousemove=dragDiv;

          }
          function dragDiv(e) {
              if (!drag) {return};
              if (!e) { var e= window.event};
              var targ=e.target?e.target:e.srcElement;
              // move div element
              targ.style.left=coordX+e.clientX-offsetX+'px';
              targ.style.top=coordY+e.clientY-offsetY+'px';
              return false;
          }
          function stopDrag() {
              drag=false;
          }
          window.onload = function() {
              document.onmousedown = startDrag;
              document.onmouseup = stopDrag;
          }
  </script>

  <img src="flower.jpg" alt="drag-and-drop image script"
   title="drag-and-drop image script" class="dragme">

   <div class="row">
     <div class="column">
       <div class="dragme" id="diagraphs" title="sh">sh</div>
     </div>
     <div class="column">
       <div class="dragme" id="diagraphs" title="ch">ch</div>
     </div>
     <div class="column">
       <div class="dragme" id="diagraphs" title="th">th</div>
     </div>
     <div class="column">
       <div class="dragme" id="diagraphs">wh</div>
     </div>
     <div class="column">
       <div class="dragme" id="diagraphs">ph</div>
    </div>
  </div>

  <link rel="stylesheet" type="text/css" href="index.css">.
</html-->
